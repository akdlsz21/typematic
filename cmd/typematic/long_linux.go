//go:build linux

package main

const longDesc = "typematic sets the keyboard auto-repeat delay and rate on Linux GNOME. Requires GNOME on Wayland with gsettings and a session bus (DBUS_SESSION_BUS_ADDRESS). --show prints delay_ms, interval_ms, and rate_cps."

